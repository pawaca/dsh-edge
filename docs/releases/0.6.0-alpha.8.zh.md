## dsh-edge 0.6.0-alpha.8

移除启动时 repack 迁移；0.6.0 正式版前最后一个 alpha。

### 移除

- **启动时 repack**：`repackExistingChunks` 迁移在每次 DO 冷启动时运行，压缩旧的 `assistant/chunk` 行。它导致了部分提交的数据丢失，且仅对 alpha.5 之前的数据有用。新写入已通过 `appendBatch` 自动打包；旧 session 通过提高的 `maxEvents` 限制（65,536）正常加载。
