# Agent Note: dsh-edge 0.4 multi-provider vision

Status: active — Stage 3 completed, release prep in progress

English | [中文](2026-08-22-dsh-edge-v0.4-multi-provider-vision.zh.md)

## Outcome

dsh-edge 0.4 adds runtime-configurable LLM providers and writable credentials, enabling users to configure DeepSeek and OpenAI-compatible providers from the browser Settings → Models page without redeployment. The upstream `dsh-llm-deepseek` cordis plugin, Settings subsystem, and credential-provider seam handle provider management; dsh-edge composes a Durable Object storage backend for both settings persistence and credential storage.

Existing deployments remain backward compatible: Worker environment secrets continue to work as a fallback source, and no migration is required.

## Non-negotiable ownership boundary

For every capability, use this order and record why a lower choice was necessary:

1. Consume the published upstream package, plugin, UI, protocol, schema, and default catalog unchanged.
2. Compose an upstream service/provider/plugin slot.
3. Implement the smallest Edge-owned adapter behind an upstream interface.
4. Add a `ui-edge` contribution only for Cloudflare-specific product state or actions.
5. Apply an exact-version package patch only when composition cannot express the requirement.
6. Never copy or independently reimplement an upstream product capability.

Every delivery PR must answer:

- Which upstream capability is reused?
- Why is each new Edge-owned code path not expressible by composition alone?
- Did the PR add or retain a package patch, and what removes it?

## Credential invariant update

The v0.3 invariant "Never persist or log `DEEPSEEK_API_KEY`" is replaced by:

> Provider credentials persist only through the upstream `CredentialProvider` seam in Durable Object storage; resolved values remain request-scoped in the LLM adapter and are never logged, cached across requests, or written to session events.

This enables the upstream Settings → Models page to accept API keys at runtime while maintaining the request-scoped resolution contract that upstream consumers depend on.

## Scope

### Included

- Writable `EdgeCredentialProvider` with DO KV storage and Worker env fallback.
- Upstream `dsh-settings` integration via a `DurableObjectSettingsProvider`.
- Upstream `dsh-llm-deepseek` installed as a cordis plugin (auto-registers `llm-deepseek` settings namespace and configurable provider entry).
- `ApiProxy` settings and credentials wiring so the upstream Models page UI works unchanged.
- Deployment defaults aligned with upstream plugin (maxTokens=256000, reasoningEffort=high); only env-var overrides are passed.
- Bilingual documentation updates.

### Excluded

- Auth delegation to Durable Object (deferred; access key changes require a separate architectural change).
- OAuth and interactive authorization flows (`ctx.authorization`).
- Non-OpenAI-compatible API protocols (native Anthropic Messages API, Bedrock, Vertex). `dsh-llm-pi-ai` bundles Node.js-only SDKs incompatible with Workers and exceeds the gzip budget.
- Credential record storage (`readRecord`, `modifyRecord`).
- Multi-provider parallel registration (upstream plugin's provider ID is hardcoded; switching provider endpoint is done through Settings baseURL).
- A second credential UI, provider form, or model catalog owned by dsh-edge.

## Delivery stages

| Stage | Delivery | Exit criteria | Status |
| --- | --- | --- | --- |
| 1 | Writable credentials with DO storage | `set`/`unset` persist to DO KV; `resolve` checks DO first then Worker env; existing deployments keep working; tests pass | Complete 2026-08-22 |
| 2 | Upstream Settings integration | `DurableObjectSettingsProvider` implements `load`/`persist`; ApiProxy settings wired to real implementation; Settings page opens and reads current provider | Complete 2026-08-22 |
| 3 | Upstream LLM plugin | `dsh-llm-deepseek` installed as cordis plugin; `EdgeDeepSeekAdapter` and `EdgeProviderManager` deleted; `llm-deepseek` settings namespace auto-registered; Edge defaults preserved; all snapshots match | Complete 2026-08-23 |
| 4 | Documentation and release | Bilingual docs updated; version bumped to 0.4.0-alpha.1; Agent Note updated | In progress |

Each stage is normally one reviewable PR. Stage 3 pivoted from an Edge-owned `EdgeProviderManager` approach (PR #32) to the upstream `dsh-llm-deepseek` plugin (PR #33) based on the principle of maximizing upstream capability reuse.

## Stage 1 evidence

### Upstream reuse

- `EdgeCredentialProvider` extends `@deepseek-ai/dsh-credentials/CredentialProvider` — the same abstract seam used by upstream `dsh-credentials-local`.
- The `resolve`/`describe`/`set`/`unset` contract matches upstream semantics: per-operation resolution, empty values treated as absent, source-layer reporting.
- `dsh-settings` v0.1.1-rc.1 is already installed and partially bundled (schemastery shared by 11 packages).

### Edge-owned code

- `DurableObjectStorage` KV is the persistence medium because no filesystem or `dsh-settings-file` is available in Workers.
- The storage key prefix `dsh-edge:credential:` follows the same naming convention as `dsh-edge:workspace-domain-state:` and `dsh-edge:session-model:`.
- Worker env fallback is Edge-specific: upstream providers resolve from env vars, files, or project/user env layers; Edge collapses this to one env fallback.

### Backward compatibility

- Existing deployments that set `DEEPSEEK_API_KEY` via `wrangler secret put` continue to work: `resolve()` falls back to Worker env when DO KV is empty.
- `describe()` now reports `writable: true` instead of `false`. This change enables the upstream Models page to show the "Save" button for credentials. Old clients that don't call `set()` are unaffected.

## Stage 3 architectural pivot

PR #32 initially implemented an Edge-owned `EdgeProviderManager` that stored provider entries in a separate DO KV key (`dsh-edge:configured-providers`). Codex review identified that this approach bypassed the upstream Settings namespace and created a disconnected storage path that the upstream Models UI couldn't reach.

After researching the upstream `dsh-llm-deepseek` plugin, we found it is a complete cordis plugin that auto-registers its `llm-deepseek` settings namespace and configurable provider entry. PR #33 replaced the Edge-owned approach with the upstream plugin, resulting in a net deletion of 449 lines (removed `EdgeDeepSeekAdapter`, `EdgeProviderManager`, and their tests).

The `dsh-llm-pi-ai` package was evaluated for multi-protocol support but is not feasible: its `@earendil-works/pi-ai` dependency bundles Node.js-only SDKs (Anthropic, Google, OpenAI) and `http-proxy-agent`, which cannot run in Cloudflare Workers and would exceed the 900 KB gzip budget.

## Progress log

| Date | Change | Evidence / next action |
| --- | --- | --- |
| 2026-08-22 | Stage 1 completed | EdgeCredentialProvider writable with DO KV + Worker env fallback; credentials.set/unset wired through ApiProxy; AGENTS.md invariant updated; 255 tests pass |
| 2026-08-22 | Stage 2 completed | DurableObjectSettingsProvider installed; ApiProxy settings methods wired; tests exercise provider through real cordis Context; 248 tests pass |
| 2026-08-23 | Stage 3 completed | EdgeDeepSeekAdapter and EdgeProviderManager replaced with upstream dsh-llm-deepseek plugin; deployment defaults aligned with upstream (maxTokens=256000, reasoningEffort=high); all snapshots match; 262 tests pass |
| 2026-08-23 | Stage 4 started | Version bumped to 0.4.0-alpha.1; bilingual docs updated; release prep in progress |
