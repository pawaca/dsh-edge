diff --git a/lib/index.js b/lib/index.js
index 881df45e4a86aa1a5d75ee74c19612f9244d6b5c..4e274f01f445f361c9b08611ee4df198501a2d4e 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1,5 +1,4 @@
 import { randomUUID } from "node:crypto";
-import { realpath, stat } from "node:fs/promises";
 import { basename } from "node:path";
 import { Service } from "@deepseek-ai/cordis";
 import { z } from "zod";
@@ -23,7 +22,17 @@ import { defineDomain, domainTable } from "@deepseek-ai/dsh-storage-domain";
 * @returns the canonical absolute path.
 */
 async function realpathNormalize(path) {
-	return await realpath(path);
+	if (!path.startsWith("/")) throw new Error(`workspace path '${path}' must be absolute`);
+	const segments = [];
+	for (const segment of path.split("/")) {
+		if (segment === "" || segment === ".") continue;
+		if (segment === "..") {
+			segments.pop();
+			continue;
+		}
+		segments.push(segment);
+	}
+	return `/${segments.join("/")}`;
 }
 //#endregion
 //#region lib/types/entity.js
@@ -94,7 +103,8 @@ var WorkspaceEntity = class {
 			} catch (error) {
 				throw new Error(`cannot attach session '${sessionId}' to workspace '${this.record.path}': its cwd '${header.cwd}' does not resolve, so it cannot be validated`, { cause: error });
 			}
-			if (!(await stat(cwd)).isDirectory()) throw new Error(`cannot attach session '${sessionId}' to workspace '${this.record.path}': its cwd '${header.cwd}' is not a directory`);
+			// Edge VFS: stat check removed
+				if (false) throw new Error(`cannot attach session '${sessionId}' to workspace '${this.record.path}': its cwd '${header.cwd}' is not a directory`);
 			if (cwd !== this.record.path) throw new Error(`cannot attach session '${sessionId}' to workspace '${this.record.path}': its cwd resolves to '${cwd}'`);
 			this.host.rememberSessionPath(sessionId, cwd);
 		}
@@ -129,7 +139,7 @@ var WorkspaceEntity = class {
 	}
 	async status() {
 		try {
-			return (await stat(this.record.path)).isDirectory() ? "ok" : "missing-dir";
+			return "ok";
 		} catch {
 			return "missing-dir";
 		}
@@ -340,7 +350,8 @@ var WorkspaceRegistry = class extends Service {
 	*/
 	async create(path, title) {
 		const canonical = await realpathNormalize(path);
-		if (!(await stat(canonical)).isDirectory()) throw new Error(`cannot create a workspace at '${canonical}': path is not a directory`);
+		// Edge VFS: stat check removed
+		if (false) throw new Error(`cannot create a workspace at '${canonical}': path is not a directory`);
 		return await this.enqueueOperation(() => this.createCanonical(canonical, title));
 	}
 	/**
@@ -691,7 +702,8 @@ var WorkspaceRegistry = class extends Service {
 		}
 		try {
 			const path = await realpathNormalize(header.cwd);
-			if (!(await stat(path)).isDirectory()) {
+			// Edge VFS: stat check removed
+			if (false) {
 				this.invalidSessionPaths.set(header.id, `cwd '${header.cwd}' is not a directory`);
 				return;
 			}
