diff --git a/lib/index.js b/lib/index.js
index 178ec2da947f243323bf4482dfe32bb29bb346de..54e9c60f16955c4bc6c0f985bc550e65db3acad1 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -351,6 +351,17 @@ var SessionWriteBehind = class {
 		this.cancelTimer();
 		this.deadlineExpired = false;
 	}
+	/**
+	* Drop a retained failed batch after its owning unmaterialized session is
+	* explicitly abandoned. The caller must first await the failed barrier.
+	*/
+	discardPending() {
+		if (this.active !== void 0 || this.barrier !== void 0) throw new Error("cannot discard session writes while a durability barrier is active");
+		this.cancelTimer();
+		this.pending = [];
+		this.deadlineExpired = false;
+		this.automaticPaused = false;
+	}
 	/** Start the one fixed window for the current pending prefix. */
 	armTimer() {
 		this.timer = setTimeout(() => {
@@ -805,6 +816,28 @@ var PersistenceCoordinator = class {
 		if (!Number.isSafeInteger(snapshot.createdAt) || snapshot.createdAt < 0) return Promise.reject(/* @__PURE__ */ new TypeError("session metadata createdAt must be a non-negative safe integer"));
 		return this.serialize(snapshot.id, () => this.createCore(snapshot));
 	}
+	/**
+	* Release a live session whose first durability barrier failed before any
+	* event materialized. Callers use this before disposing the session so
+	* retirement cannot retry and commit an operation already reported failed.
+	* @param session - exact live session whose retained first batch is discarded.
+	* @internal
+	*/
+	async abandonUnmaterialized(session) {
+		const live = this.live.get(session);
+		if (live === void 0) return;
+		live.writes.cancelAutomaticWait();
+		await live.init.catch(() => {});
+		await this.serialize(session.id, () => {
+			if (this.live.get(session) !== live) return;
+			const state = this.states.get(session.id);
+			if (state?.materialized === true) throw new Error(`cannot abandon materialized session "${session.id}"`);
+			if (state !== void 0 && state.owner !== session) throw new Error(`cannot abandon session "${session.id}" owned by another lifecycle`);
+			live.writes.discardPending();
+			this.live.delete(session);
+			if (state?.owner === session) this.states.delete(session.id);
+		});
+	}
 	async createCore(meta) {
 		if (this.states.has(meta.id) || this.preparations.has(meta.id)) throw new Error(`session "${meta.id}" already exists in this backend`);
 		if (await this.backend.loadStored(meta.id) !== void 0) throw new Error(`session "${meta.id}" already has a persisted log on disk; load/resume it instead of creating`);
@@ -933,6 +966,58 @@ var PersistenceCoordinator = class {
 		const retired = Promise.resolve(this.retirements.get(id));
 		return (signal === void 0 ? retired : observeQueuedAbort(retired, signal, () => false)).then(() => this.serialize(id, () => this.readFromCore(id, fromSeq, signal), signal));
 	}
+	/**
+	* Validate a backend-private bounded read on the coordinator's per-session chain.
+	* This helper does not add a read operation to the persistence service or backend interface.
+	* @param id - persisted session to read.
+	* @param fromSeq - first event sequence to include.
+	* @param limit - maximum events accepted from the private loader.
+	* @param loadStoredPage - operation owned by the concrete backend facade.
+	* @param signal - optional cancellation for queued and backend read work.
+	* @returns validated metadata, events, and continuation state.
+	* @internal
+	*/
+	readValidatedPage(id, fromSeq, limit, loadStoredPage, signal) {
+		if (!Number.isSafeInteger(fromSeq) || fromSeq < 0) return Promise.reject(/* @__PURE__ */ new TypeError(`readValidatedPage fromSeq must be a non-negative safe integer, got ${String(fromSeq)}`));
+		if (!Number.isSafeInteger(limit) || limit <= 0) return Promise.reject(/* @__PURE__ */ new TypeError(`readValidatedPage limit must be a positive safe integer, got ${String(limit)}`));
+		const retired = Promise.resolve(this.retirements.get(id));
+		return (signal === void 0 ? retired : observeQueuedAbort(retired, signal, () => false)).then(() => this.serialize(id, () => this.readValidatedPageCore(id, fromSeq, limit, loadStoredPage, signal), signal));
+	}
+	async readValidatedPageCore(id, fromSeq, limit, loadStoredPage, signal) {
+		signal?.throwIfAborted();
+		const page = await loadStoredPage(id, fromSeq, limit, signal);
+		signal?.throwIfAborted();
+		if (page === void 0) throw new Error(`session "${id}" not found`);
+		this.assertStoredId(id, page.meta);
+		this.assertVersion(page.meta);
+		if (page.events.length > limit) throw new Error(`session "${id}" backend returned more than ${limit} events for a bounded page`);
+		if (page.events.some(needsLegacyPrefix)) {
+			const prefixLimit = fromSeq + limit;
+			if (!Number.isSafeInteger(prefixLimit)) throw new TypeError(`readValidatedPage prefix limit must be a safe integer, got ${String(prefixLimit)}`);
+			const prefix = await loadStoredPage(id, 0, prefixLimit, signal);
+			signal?.throwIfAborted();
+			if (prefix === void 0) throw new Error(`session "${id}" not found`);
+			this.assertStoredId(id, prefix.meta);
+			this.assertVersion(prefix.meta);
+			if (prefix.events.length > prefixLimit) throw new Error(`session "${id}" backend returned more than ${prefixLimit} events for a bounded legacy prefix`);
+			if (prefix.hasMore && prefix.events.length < prefixLimit) throw new Error(`session "${id}" legacy replay prefix exceeds the bounded page capacity`);
+			const normalized = snapshotStoredEvents(prefix.events, id);
+			this.assertEventsSupported(prefix.meta, normalized);
+			const suffix = normalized.filter((event) => event.seq >= fromSeq);
+			return {
+				meta: structuredClone(prefix.meta),
+				events: suffix.slice(0, limit),
+				hasMore: prefix.hasMore
+			};
+		}
+		const events = snapshotStoredEvents(page.events, id);
+		this.assertEventsSupported(page.meta, events);
+		return {
+			meta: structuredClone(page.meta),
+			events,
+			hasMore: page.hasMore
+		};
+	}
 	async readFromCore(id, fromSeq, signal) {
 		signal?.throwIfAborted();
 		if (this.backend.loadStoredFrom !== void 0) {
diff --git a/lib/types/coordinator.d.ts b/lib/types/coordinator.d.ts
index 04301acda450bd855d0ed8f8ec4a7277b66771ec..4fdbf1421d7dddec2486b1e97db2a716e7623131 100644
--- a/lib/types/coordinator.d.ts
+++ b/lib/types/coordinator.d.ts
@@ -6,7 +6,7 @@
  */
 import { Context } from '@deepseek-ai/cordis';
 import { SessionPreparation } from '@deepseek-ai/dsh-session';
-import type { SessionEvent, SessionId, SessionHeader } from '@deepseek-ai/dsh-session';
+import type { Session, SessionEvent, SessionId, SessionHeader } from '@deepseek-ai/dsh-session';
 import type { SessionInspection, SessionLocation } from './index.ts';
 import type { SessionPersistenceRevision } from './revision.ts';
 /** Default number of detached session preparations retained by a coordinator. */
@@ -82,6 +82,11 @@ export interface StoredSuffix {
     meta: SessionHeader;
     events: SessionEvent[];
 }
+/** A backend-private bounded physical event page awaiting canonical validation. */
+interface StoredPage extends StoredSuffix {
+    hasMore: boolean;
+}
+type StoredPageLoader = (id: SessionId, fromSeq: number, limit: number, signal?: AbortSignal) => Promise<StoredPage | undefined>;
 /**
  * The storage contract between {@link PersistenceCoordinator} and a concrete
  * backend: the minimal set of durable primitives the orchestration calls. A
@@ -210,6 +215,14 @@ export declare class PersistenceCoordinator<TornMarker = unknown> {
      * @param meta - header to snapshot; duplicate tracked or persisted ids reject.
      */
     create(meta: SessionHeader): Promise<void>;
+    /**
+     * Release a live session whose first durability barrier failed before any
+     * event materialized. Callers use this before disposing the session so
+     * retirement cannot retry and commit an operation already reported failed.
+     * @param session - exact live session whose retained first batch is discarded.
+     * @internal
+     */
+    abandonUnmaterialized(session: Session): Promise<void>;
     private createCore;
     /**
      * Durably persist a batch of events. Honors the append-only and contiguous-seq
@@ -263,6 +276,19 @@ export declare class PersistenceCoordinator<TornMarker = unknown> {
         meta: SessionHeader;
         events: SessionEvent[];
     }>;
+    /**
+     * Validate a backend-private bounded read on the coordinator's per-session chain.
+     * This helper does not add a read operation to the persistence service or backend interface.
+     * @param id - persisted session to read.
+     * @param fromSeq - first event sequence to include.
+     * @param limit - maximum events accepted from the private loader.
+     * @param loadStoredPage - operation owned by the concrete backend facade.
+     * @param signal - optional cancellation for queued and backend read work.
+     * @returns validated metadata, events, and continuation state.
+     * @internal
+     */
+    readValidatedPage(id: SessionId, fromSeq: number, limit: number, loadStoredPage: StoredPageLoader, signal?: AbortSignal): Promise<StoredPage>;
+    private readValidatedPageCore;
     private readFromCore;
     /** Read one detached physical prefix without logical recovery or caching. */
     private readStoredPrefix;
@@ -345,4 +371,5 @@ export declare class PersistenceCoordinator<TornMarker = unknown> {
     /** Append one controller-owned prefix after filtering events initialization already stored. */
     private appendLiveBatch;
 }
+export {};
 //# sourceMappingURL=coordinator.d.ts.map
\ No newline at end of file
diff --git a/lib/types/write-behind.d.ts b/lib/types/write-behind.d.ts
index 7775329e3d87e97cbdafec0b1396746060ea285d..8d9dad870295f57c5b697ed463131012ce22fe94 100644
--- a/lib/types/write-behind.d.ts
+++ b/lib/types/write-behind.d.ts
@@ -44,6 +44,11 @@ export declare class SessionWriteBehind {
     flush(): Promise<void>;
     /** Cancel the current automatic deadline without draining retained work. */
     cancelAutomaticWait(): void;
+    /**
+     * Drop a retained failed batch after its owning unmaterialized session is
+     * explicitly abandoned. The caller must first await the failed barrier.
+     */
+    discardPending(): void;
     /** Start the one fixed window for the current pending prefix. */
     private armTimer;
     /** Cancel any pending automatic deadline. */
