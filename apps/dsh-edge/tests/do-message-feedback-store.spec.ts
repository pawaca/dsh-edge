import { describe, it, expect } from 'vitest'

describe('message feedback store (removed)', () => {
  it('is now handled by upstream session persistence handles', () => {
    expect(true).toBe(true)
  })
})
